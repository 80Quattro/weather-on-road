import { Typography } from '@mui/material';
import React from 'react';

const Logo = () => {
    return ( 
        <Typography variant='h5' sx={{fontVariantCaps: 'all-small-caps'}}>
            Weather On The Road
        </Typography>
     );
}
 
export default Logo;